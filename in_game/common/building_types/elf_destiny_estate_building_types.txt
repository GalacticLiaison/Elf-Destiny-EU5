
# local_max_rgo_size
# local_manpower_modifier
# local_build_buildings_cost
# movement_cost
# fort_level
# propagating_zone_of_control
# local_cultural_tradition
# local_cultural_influence
# local_pop_promotion_speed(scaled)
# local_migration_attraction
# local_pop_conversion_speed
# local_pop_assimilation_speed
# local_raw_material_output
# local_monthly_prosperity
# local_monthly_development
# local_monthly_development_modifier
# local_max_control
# local_monthly_control
# local_population_growth
# local_food_capacity_modifier
# local_army_attrition
# supply_limit
# local_supply_limit_modifier
# max_attrition
# local_market_access
# local_defensive
# local_monthly_literacy
# local_max_literacy
# local_institution_growth_modifier
# local_levy_size_modifier
# local_army_levy_size_modifier
# local_production_efficiency
# local_unrest
# local_wine_output_modifier
# local_incense_output_modifier
# maximum_stockpile_capacity
# local_calaquendi_estate_power
# local_pop_food_consumption


# Mallorn Tree Grove
# A collection of ancient mallorn trees. Only those who have seen the light of both Trees are permitted to sit among them; Thrones rise on living roots to whatever height their lord desires.

mallorn_tree_grove = {
	category = estate_category
	is_foreign = no
	max_levels = 1

	pop_type = calaquendi
	employment_size = 0.03
	
	rural_settlement = no
	town = yes
	city = yes
	
	estate = calaquendi_estate
	
	build_time = estate_build_time
	
	remove_if = {
		elf_building_removal_trigger = yes
	}
	
	possible_production_methods = {
		estate_building_input
	}

	construction_demand = estate_construct_building
	
	modifier = {
		local_monthly_prosperity = 0.001
		local_monthly_food_modifier = 0.05
	}

	on_built = {
		owner = {
			add_country_modifier = { 
				modifier = greater_religious_influence_bonus
				mode = add_and_extend
			}
		}
	}

	on_destroyed = {
		owner = {
			remove_country_modifier = greater_religious_influence_bonus
		}
	}
	
	graphical_tags = { small_religious }
}

# Baths of Starlight
# A marble pool filled not with water but with captured liquid starlight that warms the body and slows the fading of the spirit. Attendants pour it from phials of the purest silima.

baths_of_starlight = {
	category = estate_category
	is_foreign = no
	max_levels = 1

	pop_type = calaquendi
	employment_size = 0.03
	
	rural_settlement = yes
	town = yes
	city = yes
	
	estate = calaquendi_estate
	
	build_time = estate_build_time

	remove_if = {
		elf_building_removal_trigger = yes
	}
	
	possible_production_methods = {
		estate_building_input
	}

	construction_demand = estate_construct_building
	
	modifier = {
		# monthly_religious_influence = 0.1
		local_unrest = -0.05
		local_migration_attraction = 0.025
	}

	on_built = {
		owner = {
			add_country_modifier = { 
				modifier = greater_religious_influence_bonus
				mode = add_and_extend
			}
		}
	}

	on_destroyed = {
		owner = {
			remove_country_modifier = greater_religious_influence_bonus
		}
	}
	
	graphical_tags = { small_religious }
}


# Perfume garden of Valinor
# A terraced garden where every flower brought from Aman still grows. The mingled scents are so potent that mortals fall into blissful sleep for days; Calaquendi breathe it like wine.

perfume_garden_of_valinor = {
	category = estate_category
	is_foreign = no
	max_levels = 1

	pop_type = calaquendi
	employment_size = 0.03
	
	rural_settlement = yes
	town = yes
	city = yes
	
	estate = calaquendi_estate
	
	build_time = estate_build_time
	
	remove_if = {
		elf_building_removal_trigger = yes
	}

	possible_production_methods = {
		estate_building_input
	}

	construction_demand = estate_construct_building
	
	modifier = {
		local_incense_output_modifier = 0.2
	}

	on_built = {
		owner = {
			add_country_modifier = { 
				modifier = greater_religious_influence_bonus
				mode = add_and_extend
			}
		}
	}

	unique_production_methods = {
		perfume_garden_production = {
			liquor = 0.2
			fruit = 0.25

			produced = incense
			output = 1
			
			debug_max_profit = 0.6
			category = guild_input
		}
	}

	on_destroyed = {
		owner = {
			remove_country_modifier = greater_religious_influence_bonus
		}
	}
	
	graphical_tags = { small_religious }
}

# Wine-Cellar of the Elder Days
# A subterranean vault where amphorae from the feasts of the Valar are still sealed. One sip grants memories of the Noontide of Valinor; only the greatest lords are offered a single cup in a lifetime.

wine_cellar_of_elder_days = {
	category = estate_category
	is_foreign = no
	max_levels = 1

	pop_type = calaquendi
	employment_size = 0.03
	
	rural_settlement = yes
	town = yes
	city = yes
	
	estate = calaquendi_estate
	
	build_time = estate_build_time

	remove_if = {
		elf_building_removal_trigger = yes
	}
	
	possible_production_methods = {
		estate_building_input
	}

	construction_demand = estate_construct_building
	
	modifier = {
		local_wine_output_modifier = 0.2
	}

	on_built = {
		owner = {
			add_country_modifier = { 
				modifier = greater_religious_influence_bonus
				mode = add_and_extend
			}
		}
	}

	unique_production_methods = {
		wine_cellar_production_fruit = {
			fruit = 1.25
			lumber = 0.17
			tools = 0.1

			produced = wine
			output = 1
			
			debug_max_profit = 0.6
			category = guild_input
		}

		wine_cellar_production_multiply = {
			wine = 0.75
			fruit = 0.25

			produced = wine
			output = 2
			
			debug_max_profit = 0.6
			category = guild_input
		}
	}

	on_destroyed = {
		owner = {
			remove_country_modifier = greater_religious_influence_bonus
		}
	}
	
	graphical_tags = { small_religious }
}

# Feasting Hall of the Trees
# A vaulted hall whose ceiling is one living mallorn canopy of gold and silver leaves. Tables are white stone inlaid with gems in the shape of the Two Trees; food and wine never diminish while the feast-song lasts.

feasting_hall_of_the_trees = {
	category = estate_category
	is_foreign = no
	max_levels = 1

	pop_type = calaquendi
	employment_size = 0.03
	
	rural_settlement = yes
	town = yes
	city = yes
	
	estate = calaquendi_estate
	
	build_time = estate_build_time

	remove_if = {
		elf_building_removal_trigger = yes
	}
	
	possible_production_methods = {
		estate_building_input
	}

	construction_demand = estate_construct_building
	
	modifier = {
		local_pop_food_consumption = 0.1
		local_unrest = -0.05
		local_supply_limit_modifier = 0.05
		local_food_capacity_modifier = 0.5
	}

	on_built = {
		owner = {
			add_country_modifier = { 
				modifier = greater_religious_influence_bonus
				mode = add_and_extend
			}
		}
	}

	on_destroyed = {
		owner = {
			remove_country_modifier = greater_religious_influence_bonus
		}
	}
	
	graphical_tags = { small_religious }
}

# Grand Archives
# A great repository of lore and wisdom preserved and protected.

grand_archives = {
	category = estate_category
	is_foreign = no
	max_levels = 1

	pop_type = calaquendi
	employment_size = 0.03
	
	rural_settlement = yes
	town = yes
	city = yes
	
	estate = calaquendi_estate
	
	build_time = estate_build_time

	remove_if = {
		elf_building_removal_trigger = yes
	}
	
	possible_production_methods = {
		estate_building_input
	}

	construction_demand = estate_construct_building
	
	modifier = {
		local_max_literacy = 5
		local_monthly_literacy = 0.1
		local_cultural_tradition = 0.2
	}

	on_built = {
		owner = {
			add_country_modifier = { 
				modifier = greater_religious_influence_bonus
				mode = add_and_extend
			}
		}
	}

	on_destroyed = {
		owner = {
			remove_country_modifier = greater_religious_influence_bonus
		}
	}
	
	graphical_tags = { small_religious }
}