# elven_high_councils = {
# 	age = age_1_traditions
	
# 	societal_values = {
# 		humanist_focus
# 	}

# 	country_modifier = {
# 		tolerance_heretic = 1
# 		tolerance_heathen = 1
# 		monthly_towards_communalism = societal_value_monthly_move
# 		monthly_towards_humanist = societal_value_monthly_move
# 	}

# 	years = 2
# }



# # Note: give +1 government reforms for these
elven_superiority = {
	major = no # is this a major reform? a country cannot select another major reform if they already have a major reform. default = no

	age = age_1_traditions # which age is this available from?

	# government = monarchy # which government is allowed to use this

	potential = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		has_global_variable = human_relations_chosen
		# what triggers must be true for this to even be visible in the UI. root = country
	}

	allow = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		has_global_variable = human_relations_chosen
		NOT = {
			has_reform = government_reform:open_hearted
		}
		# what triggers must be true for this to even be selectable. root = country
	}	
	
	locked = {
        always = yes
		# when these triggers are true, the reform cannot be manually removed by the country. root = country
	}
	
	country_modifier = {
        government_reform_slots = 1
		# new casus belli to raid for slaves in places that arent majority elf
		auto_slave_raid = yes
		slave_raid_efficiency = 0.25
		diplomatic_reputation = -1
		monthly_prestige = 0.2
		raid_for_humans = yes
	}
	
	location_modifier = {

	}
	
	days = 0
}

open_hearted = {
	major = no # is this a major reform? a country cannot select another major reform if they already have a major reform. default = no

	age = age_1_traditions # which age is this available from?

	# government = monarchy # which government is allowed to use this

	potential = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		has_global_variable = human_relations_chosen
		# what triggers must be true for this to even be visible in the UI. root = country
	}

	allow = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		has_global_variable = human_relations_chosen
		NOT = {
			has_reform = government_reform:elven_superiority
		}
		# what triggers must be true for this to even be selectable. root = country
	}	
	
	locked = {
        always = yes
		# when these triggers are true, the reform cannot be manually removed by the country. root = country
	}
	
	country_modifier = {
        government_reform_slots = 1
		diplomatic_reputation = 2
		research_speed_modifier = 0.025
		# any_accepted_culture
		# any_present_culture_in_country
		# any_primary_or_accepted_tolerated_culture
		# any_tolerated_culture
		# culture_group_percentage_in_country
	}
	
	location_modifier = {
		# conditional check to increase pop growth in locations with both elf and human cultures present?

		# culture_group_percentage

		#conditional location modifiers. Can add multiple of these
		potential_trigger = {
            custom_tooltip = {
				text = location_has_both_human_and_elf_pops
				# Humans + Elves live together
				any_pop = { # Elf Culture Present
					culture = {has_culture_group = culture_group:elven_culture_group} 
				}
				# +
				any_pop = { # Human Culture Present
					culture = {
						NOT = {has_culture_group = culture_group:elven_culture_group}
					} 
				}
			}
        }
		local_population_growth = 0.02
		local_migration_attraction = 0.3
		local_pop_promotion_speed_modifier = 0.1
	}
	
	# male_regnal_names = {
	# 	# new male rulers will randomly select one of these names to use
	# 	David
	# }
	
	# female_regnal_names = {
	# 	# new female rulers will randomly select one of these names to use
	# 	David
	# }

	days = 2
}


# calaquendi_mentorship = {
# 	all children recieve expensive education
# 	calaquendi power grows
# }





# Aelurans

# aeluran_advisors = {

# }

# daughters of the red - right to take magically talented daughters to be trained


# king_worship = {

# }

masters_of_woodland_realms = {
	major = yes # is this a major reform? a country cannot select another major reform if they already have a major reform. default = no

	age = age_1_traditions # which age is this available from?

	# government = monarchy # which government is allowed to use this

	potential = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		# what triggers must be true for this to even be visible in the UI. root = country
	}

	allow = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		# what triggers must be true for this to even be selectable. root = country
	}	
	
	locked = {
        # always = yes
		# when these triggers are true, the reform cannot be manually removed by the country. root = country
	}
	
	country_modifier = {
		#conditional country modifiers. Can add multiple of these
		#optional trigger = {}
        global_wild_game_output_modifier = 0.2
        global_fur_output_modifier = 0.2
        global_lumber_output_modifier = 0.2
        government_reform_slots = 1
        # aeluran_advisor_appointed = yes
	}
	
	location_modifier = {
		#conditional location modifiers. Can add multiple of these
		potential_trigger = {
            custom_tooltip = {
				text = is_a_woodland_realm
                OR = {
                    vegetation = forest
                    vegetation = woods
                }
			}
        }
		local_monthly_food_modifier = 0.15
        local_monthly_prosperity = 0.0015
	}
	
	# male_regnal_names = {
	# 	# new male rulers will randomly select one of these names to use
	# 	David
	# }
	
	# female_regnal_names = {
	# 	# new female rulers will randomly select one of these names to use
	# 	David
	# }

	days = 2
	# weeks = x
	# months = x
	# years = 2 #how long it takes before the reform is fully implemented. Leave unset for instant implementation
}

people_of_the_bow = {
	# major = yes # is this a major reform? a country cannot select another major reform if they already have a major reform. default = no

	age = age_1_traditions # which age is this available from?

	# government = monarchy # which government is allowed to use this

	potential = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		# what triggers must be true for this to even be visible in the UI. root = country
	}

	allow = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		# what triggers must be true for this to even be selectable. root = country
	}	
	
	locked = {
        # always = yes
		# when these triggers are true, the reform cannot be manually removed by the country. root = country
	}
	
	country_modifier = {
		#conditional country modifiers. Can add multiple of these
		#optional trigger = {}
        government_reform_slots = 1
        army_infantry_power = 0.15
        army_cavalry_power = -0.10
        army_infantry_maintenance_cost_modifier = -0.10
        global_wild_game_output_modifier = 0.10
        ranger_high_guard_unlocked = yes
	}

	on_activate = {
		# change_heir_selection = heir_selection:admiralty_regime_heir_selection
	}
	
	# male_regnal_names = {
	# 	# new male rulers will randomly select one of these names to use
	# 	David
	# }
	
	# female_regnal_names = {
	# 	# new female rulers will randomly select one of these names to use
	# 	David
	# }

	days = 2
	# weeks = x
	# months = x
	# years = 2 #how long it takes before the reform is fully implemented. Leave unset for instant implementation
}

the_old_ways ={
	# major = yes # is this a major reform? a country cannot select another major reform if they already have a major reform. default = no

	age = age_1_traditions # which age is this available from?

	# government = monarchy # which government is allowed to use this

	potential = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		# what triggers must be true for this to even be visible in the UI. root = country
	}

	allow = {
        culture = { has_culture_group = culture_group:elven_culture_group}
		# what triggers must be true for this to even be selectable. root = country
	}	
	
	locked = {
        # always = yes
		# when these triggers are true, the reform cannot be manually removed by the country. root = country
	}
	
	country_modifier = {
		#conditional country modifiers. Can add multiple of these
		#optional trigger = {}
        army_magi_power = 0.10
		global_max_literacy = 10
		global_crown_estate_power = 0.05
	}

	on_activate = {
		# change_heir_selection = heir_selection:admiralty_regime_heir_selection
	}
	
	# male_regnal_names = {
	# 	# new male rulers will randomly select one of these names to use
	# 	David
	# }
	
	# female_regnal_names = {
	# 	# new female rulers will randomly select one of these names to use
	# 	David
	# }

	days = 2
	# weeks = x
	# months = x
	# years = 2 #how long it takes before the reform is fully implemented. Leave unset for instant implementation
}


# heroic_bravery_reform = {
# 	# ruler_must_be_commander_during_war

# }